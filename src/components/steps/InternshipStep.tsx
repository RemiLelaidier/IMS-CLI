import * as React from 'react';

import './Step.css';

export default class InternshipStep extends React.Component {
    public render() {
        return (
            <div>Stage</div>
        );
    }
}